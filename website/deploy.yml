- name: delete old and execute docker run in test node
  #hell: docker rmi my-php-app:latest
  shell: docker -rm my-php-app
  shell: docker run -dit --name my-php-app -p 8081:8080 1:5000my-php-app:"{{ tag }}"
